인하우스와 si&sm 의 차이점
결론 : 인하우스는 자신이 일을 찾아서 해야한다.(남이 시켜서 하는 것이 아니다)

솔루션 - 사용자 요구에 적합하면서 특정한 형태의 컴퓨터 소프트웨어 패키지나 응용프로그램과 연계된 문제를 처리해 주는 하드웨어 또는 소프트웨어를 의미한다. 솔루션은 사용자가 하드웨어, 소프트웨어, 서비스, 응용프로그램, 파일형식, 회사, 상표명, 운영체계 등을 일일이 구별해야 하는 어려움을 겪지 않고 원하는 작업을 가능하게 해준다.


스코프
변수가 인식될 수 있는 범위
자바스크립트는 함수 단위 스코프

호이스팅
지역변수 세팅이라는건 함수가실행이 되면 호이스팅이 일어난다는 것
- 실제 값이 들어가는 시점은 문서가 읽어지는 순간이지만 값이 셋팅되는것은 처음 문서를 빠르게 읽었을때이다.

NaN = not a number

falsy
1. false
2. 0
3. '' 빈문자열
4. undefined
5. null
6. NaN

truthy
falsy가 아닌 전부


함수가 실행이 되면!!!
1. 지역변수 세팅(지역변수 사용 준비)
2. this, arguments 준비

클로저
- 지역 내부에서 변수가 없을 시 외부에서 받아오는것
- 함수가 실행이 될때, 선언된 시점에 해당 값을 접근하는것.
- 함수 선언시점에 접근 가능한 변수

arguments
함수가 호출될때의 값

값이 귀결되는 식!!!
값을 넣어야 하는경우 (식은 값으로 귀결되므로)식을 넣어도 된다.